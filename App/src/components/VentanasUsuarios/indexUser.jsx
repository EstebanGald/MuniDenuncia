//Constante asociada a la ventada principal de user y su estructura (Y recibe estructura de funciones usadas)
const userMainWindow= () => {
    return(
        <div>
        </div>
    );
};

//Manejo de estado, estructura y formato de la ventana user
export function UserMainPage(){
    return userMainWindow();
}